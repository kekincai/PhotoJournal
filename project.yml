name: PhotoJournal
options:
  bundleIdPrefix: com.example
  deploymentTarget:
    iOS: "16.0"
  xcodeVersion: "15.0"

targets:
  PhotoJournal:
    type: application
    platform: iOS
    sources:
      - path: PhotoJournal
        excludes:
          - "Assets.xcassets"
      - path: PhotoJournal/Assets.xcassets
        type: folder
    info:
      path: PhotoJournal/Info.plist
      properties:
        UILaunchScreen: {}
        NSPhotoLibraryUsageDescription: "We need access to your photo library to read and write journal entries (captions) to your photos."
    settings:
      PRODUCT_BUNDLE_IDENTIFIER: com.example.PhotoJournal
      MARKETING_VERSION: "1.0"
      CURRENT_PROJECT_VERSION: "1"
      SWIFT_VERSION: "5.0"
      TARGETED_DEVICE_FAMILY: "1,2"
      INFOPLIST_KEY_UIApplicationSceneManifest_Generation: YES
      INFOPLIST_KEY_UIApplicationSupportsIndirectInputEvents: YES
      INFOPLIST_KEY_UILaunchScreen_Generation: YES
      INFOPLIST_KEY_UISupportedInterfaceOrientations_iPad: "UIInterfaceOrientationPortrait UIInterfaceOrientationPortraitUpsideDown UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight"
      INFOPLIST_KEY_UISupportedInterfaceOrientations_iPhone: "UIInterfaceOrientationPortrait UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight"
      GENERATE_INFOPLIST_FILE: YES
      ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
